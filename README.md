# Mini Shell

## Overview
A custom Unix-like shell implementation in C. This project demonstrates the fundamentals of process management, command parsing, and system calls in a Unix environment.

## Features
- Command execution and process forking.
- Support for basic shell functionality.
- Custom implementation of shell logic.

## Build and Run
```bash
gcc main.c -o myshell
./myshell
```
